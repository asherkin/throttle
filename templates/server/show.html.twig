{% extends 'base.html.twig' %}

{% block title %}Server{% endblock %}

{% block body %}
    <h1>Server</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ server.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ server.name }}</td>
            </tr>
            <tr>
                <th>Owner</th>
                <td>{{ include('_server_owner.html.twig', {owner: server.owner}) }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('server_index') }}">back to list</a>

    <a href="{{ path('server_edit', {'id': server.id}) }}">edit</a>

    {{ include('server/_delete_form.html.twig') }}
{% endblock %}
